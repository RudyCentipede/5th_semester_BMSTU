#ifndef ALGORITHMS_H
#define ALGORITHMS_H

#include <cstddef>
#include <cstdio>

size_t count_ones_recursive(const int *arr, size_t index);

size_t count_ones_iterative(const int *arr);

#endif
